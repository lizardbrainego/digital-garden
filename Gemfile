source 'https://rubygems.org'

# Use the GitHub Pages gem to align with GitHub's build environment
gem 'github-pages', group: :jekyll_plugins

# For local preview on newer Rubies
gem 'webrick', '~> 1.8'
